<Page x:Class="TestUnoAppNavigation.Presentation.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:TestUnoAppNavigation.Presentation"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      NavigationCacheMode="Required"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <ScrollViewer IsTabStop="True">
        <Grid utu:SafeArea.Insets="VisibleBounds">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <utu:NavigationBar x:Uid="MainPage_NavigationBar" />

            <StackPanel HorizontalAlignment="Center"
                        Grid.Row="1"
                        VerticalAlignment="Center"
                        Spacing="16">
                <Button Command="{Binding FindLocationCmdAsync}"
                        HorizontalAlignment="Stretch"
                        x:Uid="BtnFindLocation" />
                <Button HorizontalAlignment="Stretch"
                        x:Uid="BtnFindAsset"
                        Command="{Binding FindAssetCmdAsync}" />
                <Button HorizontalAlignment="Stretch"
                        x:Uid="BtnFindOrder"
                        Command="{Binding FindOrderCmdAsync}" />
                <Button HorizontalAlignment="Stretch"
                        x:Uid="BtnCreateOrder"
                        Command="{Binding CreateOrderCmdAsync}" />
                <Button HorizontalAlignment="Stretch"
                        x:Uid="BtnViewOrder"
                        Command="{Binding ViewOrderCmdAsync}" />
            </StackPanel>

        </Grid>
    </ScrollViewer>
</Page>
